//organize function
function organizeTasks(contentRight, result) {
    const computedStyle = getComputedStyle(result);
    const borderLeftColor = computedStyle.borderLeftColor;

    
}

export {
    organizeTasks
}